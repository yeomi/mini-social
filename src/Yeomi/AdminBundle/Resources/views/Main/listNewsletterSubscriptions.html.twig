{% extends("YeomiAdminBundle::layout.html.twig") %}

{% block content %}
    <h3 class="content-title">Liste des inscriptions à la newsletter</h3>
    <table class="table table-striped table-paginator user-table">
        <thead>
            <tr>
                <th>Ref</th>
                <th>Email</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
    {% for newsletterSubscription in newsletterSubscriptions %}
        <tr>
            <th>{{ newsletterSubscription.id }}</th>
            <td>{{ newsletterSubscription.email }}</td>
            <td>
                <a class="need-confirmation" href="{{ path("yeomi_admin_delete_entity", {"type": "newsletter_subscription", "id": newsletterSubscription.id}) }}">
                    Supprimer
                </a>
            </td>
        </tr>
    {% endfor %}
        </tbody>
    </table>
{% endblock %}